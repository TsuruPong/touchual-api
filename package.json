{
  "name": "touchual-apollo",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --format=cjs --minify",
    "zip": "powershell -Command \"Copy-Item dist/index.js index.js; npx bestzip zip/api.zip index.js node_modules; Remove-Item index.js\"",
    "packing": "npm-run-all --serial build zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "TsuruPong",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.835.0",
    "aws-lambda": "^1.0.7",
    "graphql": "^16.11.0",
    "manimani": "^1.2.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^24.0.3",
    "bestzip": "^2.2.1",
    "dotenv": "^16.5.0",
    "esbuild": "^0.25.2",
    "npm-run-all": "^4.1.5"
  }
}
